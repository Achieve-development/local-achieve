<%= form_for(@answer) do |f| %>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :question_id %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
